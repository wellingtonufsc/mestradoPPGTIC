import React from 'react';
import './Localization.scss';

const Localization = props => {
    return (
        <div className="half">
            <h1>Localização :)</h1>
        </div>
    );
}

export default Localization;